# edge/opi5-ai

Orange Pi 5 AI 服务目录。目标：提供 `/health` 和 `/api/infer/vision`，先 mock，再接真实 RKNN demo。

## 子目录

| 目录 | 用途 |
|---|---|
| `service/` | Python/C++ AI 服务 |
| `models/` | 只放 README，不提交模型权重 |
| `scripts/` | 部署和运行脚本 |

模型、数据集、人脸库和抓拍图片不入库。
