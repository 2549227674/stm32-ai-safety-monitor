# 10 设备健康监测与扩展传感器方案

## 10.1 模块定位

设备健康监测是可选扩展，不纳入 MVP。它可利用 MPU6050/GY-521 采集小风扇、水泵或云台运行时的振动，用 RMS、峰峰值等简单特征判断设备状态。

## 10.2 MPU6050 是否纳入 MVP

| 结论 | 理由 |
|---|---|
| 不纳入 MVP 必须项 | 当前主线重点是 i.MX6ULL + OPi5 + Flask 端边闭环，MPU6050 会增加 I2C 与算法工作量 |
| 作为加分项保留 | 若 GPIO/I2C/PWM/V4L2/RKNN 均顺利，可补充设备健康章节 |

## 10.3 Linux I2C 读取方案

可选实现：

- 用户态 C/C++ 使用 `/dev/i2c-*` 读取寄存器。
- Python 使用 smbus/smbus2。
- 先用 `i2cdetect` 确认设备。

> **[CLAUDE_CODE_TODO | VERIFY]** 确认 MPU6050 是否接入新硬件清单
> - 为何 GPT 给不了：用户未明确本轮一定继续使用 MPU6050，沙箱无法确认器件是否在手边。
> - 期望产物/操作：用户决定是否纳入加分项；若纳入，确认 I2C 总线、地址、电源电压。
> - 回填位置：docs/10 第 10.3；docs/12 材料表
> - 验收：明确写成“纳入/不纳入最终演示”。


## 10.4 RMS / 峰峰值算法

采样窗口：例如 1 秒 100 个样本。计算：

```text
rms = sqrt(mean(ax^2 + ay^2 + az^2))
peak_to_peak = max(norm) - min(norm)
```

阈值需基于静止、正常运行、异常振动三个场景实测。

> **[CLAUDE_CODE_TODO | MEASURE]** 标定设备健康阈值
> - 为何 GPT 给不了：阈值必须来自真实样本，不能凭空给出。
> - 期望产物/操作：分别采集静止、风扇正常、水泵/云台动作三类数据。
> - 回填位置：docs/10 第 10.4；tests/imx6ull
> - 验收：每类至少一段样本，给出 RMS/峰峰值均值。


## 10.5 与状态机结合

设备健康异常不应直接进入火灾/入侵报警，可作为 `FAULT` 或设备维护提示：

- `mpu_rms` 超过阈值：`state=FAULT` 或 `device_health=WARN`。
- 执行器动作但无振动反馈：可能负载未启动。
- 振动异常只上报 Dashboard，不控制危险执行器。

## 10.6 可选演示

将传感器固定在风扇或云台底座上，展示启动前后 RMS 变化。该演示不影响主线，可独立录屏作为加分。
