# 11 测试计划与风险矩阵（端边协同版）

## 11.1 测试原则

1. 先单模块，后集成。
2. 先空载，后接负载。
3. 先 mock，后真实 RKNN。
4. 未实测不写通过。
5. 每项测试都要有命令、现象、结果、证据路径。

## 11.2 i.MX6ULL 单模块测试

| 测试 | 命令/方法 | 结果 | 证据 |
|---|---|---|---|
| SSH 登录 | `ssh <IMX_USER>@<IMX_IP>` | 已通过：WSL/PC 到 i.MX6ULL SSH 可用 | `tests/imx6ull/2026-06-06_toolchain_ssh.md` |
| hello 运行 | `./hello_imx6ull` | 已通过：板端输出 `hello from imx6ull target` | `tests/imx6ull/2026-06-06_toolchain_ssh.md` |
| GPIO 输入 | `gpioget` 或测试程序 | 已通过（PIR/gpio117 0/1 变化） | `tests/imx6ull/2026-06-06_gpio_input_probe.md` |
| GPIO 输出 | LED/MOS 空载 | 本轮跳过（MOS 扩展项） | `tests/imx6ull/2026-06-06_mos_skip_decision.md` |
| I2C 扫描 | `i2cdetect -y 0` | 已通过（0x40 ACK） | `tests/imx6ull/2026-06-06_i2c_pca9685_probe.md` |
| PCA9685 PWM | 板端运行 | 软件准备完成（波形实测跳过） | `tests/imx6ull/2026-06-06_pca9685_pwm_empty_load.md` |
| MG90S 舵机 | 分时大幅动作 | 已通过（1100→1900us） | `tests/imx6ull/2026-06-06_pca9685_servo_small_angle.md` |
| MOS 负载 | LED/小风扇 | 本轮跳过（控制端需焊接，扩展项） | `tests/imx6ull/2026-06-06_mos_skip_decision.md` |
| V4L2 摄像头 | `v4l2-ctl` + 抓图 | 已通过（/dev/video1，MJPG 640x480，生成有效 JPEG） | `tests/imx6ull/2026-06-06_v4l2_capture.md` |


## 11.3 OPi5 AI 测试

| 测试 | 方法 | 结果 |
|---|---|---|
| SSH | `ssh <OPI5_USER>@<OPI5_IP>` | Task05-A 已通过 |
| Python 服务 | `curl /health` | Task05-A 已通过（mode=mock） |
| mock infer | 上传测试图片 | Task05-A 已通过（普通 sensors 与 mq2 hazard 分支均返回 JSON） |
| RKNN demo | 运行已有 demo | `<TODO:MEASURE>` |
| 推理延迟 | 连续 20 次 | `<TODO:MEASURE>` |

## 11.4 Flask API 测试

当前真实仓库已有 Flask 后端。扩展后需测试：

```bash
curl http://<BACKEND_HOST_TODO_FILL>:5000/health
curl -X POST http://<BACKEND_HOST_TODO_FILL>:5000/api/events -H 'Content-Type: application/json' -d @tests/payloads/event_with_ai.json
curl http://<BACKEND_HOST_TODO_FILL>:5000/api/status/latest
```

Task06 已完成本机 Flask API 与 Dashboard smoke 测试：

| 测试 | 结果 | 证据 |
|---|---|---|
| `/health` | 已通过（`ok=true`, `service=safety-monitor-backend`） | `tests/integration/2026-06-07_backend_contract_extension.md` |
| 旧事件 POST | 已通过（201，旧字段保留，扩展字段为 null） | `tests/integration/2026-06-07_backend_contract_extension.md` |
| 新 AI 事件 POST | 已通过（201） | `tests/integration/2026-06-07_backend_contract_extension.md` |
| `/api/status/latest` 扩展字段 | 已通过（`vision/ai_result/image_url/latency_ms` 可见） | `tests/integration/2026-06-07_backend_contract_extension.md` |
| `/api/events` 扩展字段 | 已通过（列表同样可见扩展字段） | `tests/integration/2026-06-07_backend_contract_extension.md` |
| Dashboard AI/视觉展示 | Playwright headless 浏览器检查通过 | `tests/integration/2026-06-07_backend_contract_extension.md` |

## 11.5 端边联调测试

完整链路验收：

| 步骤 | 期望 |
|---|---|
| 触发传感器/按键 | i.MX 状态进入 WARN/VERIFY |
| 抓拍 | 生成图片 |
| 请求 OPi5 | 返回 AI JSON |
| 融合状态机 | `risk_score` 更新 |
| 上报 Flask | 201 |
| Dashboard | 显示事件、图片、AI 摘要 |

Task07-C C 版 imx_safetyd 验收结果：

| 场景 | 结果 | 证据 |
|---|---|---|
| C 程序编译/部署 | 已通过（ARM 32-bit ELF，部署到 `/opt/edge-ai-safety-monitor/imx_safetyd`） | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| NORMAL | 已通过（gpio117 raw=0, state=NORMAL, risk_score=0, ai_status=skipped, Flask 201） | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| VERIFY | 已通过（FORCE_VERIFY=1, 抓拍成功, OPi5 HTTP 200, risk_hint=2, control_allowed=false, Flask 201） | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| loop | 已通过（2 秒周期，PIR 空闲时 POST_NORMAL=0 跳过上报） | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| systemd | 当前板端不支持（无 `systemctl`），模板已入库 | `edge/imx6ull-controller/systemd/imx-safetyd.service` |
| init.d | 已通过（`/etc/init.d/S99imx-safetyd`，`start/stop/restart/status`，`rcS` 执行 `S??*`） | `tests/integration/2026-06-07_imx_safetyd_initd.md` |

## 11.6 断网/降级测试

| 故障注入 | 期望 |
|---|---|
| 断开 OPi5 | i.MX 跳过 AI，继续本地状态机 |
| 关闭 Flask | i.MX spool 事件，不崩溃 |
| 拔掉摄像头 | 进入视觉故障，不影响传感器联动 |
| PCA9685 不响应 | 云台禁用，记录 FAULT |

Task07-C 降级验收：

| 故障注入 | 结果 | 证据 |
|---|---|---|
| OPi5 错误端口 | 已通过：生成新的 fallback AI，`ok=false/mode=offline/control_allowed=false`，`risk_score=4`，Flask 201 | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| Flask 错误端口 | 已通过：`backend_status=spooled`，写入 pending，程序不崩溃 | `tests/integration/2026-06-07_imx_safetyd_c.md` |
| flush pending | 已通过：`event_9005.json -> sent (HTTP 201)`，pending 为空，API 可见补发事件 | `tests/integration/2026-06-07_imx_safetyd_c.md` |

Task07-C2 init.d 验收：

| 场景 | 结果 | 证据 |
|---|---|---|
| 安装路径 | 已通过：`/etc/init.d/S99imx-safetyd` | `tests/integration/2026-06-07_imx_safetyd_initd.md` |
| env 路径 | 已通过：`/etc/edge-ai-safety-monitor/imx-safetyd.env` 由板端本地创建，未入库 | `tests/integration/2026-06-07_imx_safetyd_initd.md` |
| start/status | 已通过：`start` 后 PID 存在，`status` 显示 running，日志持续追加，status JSON 更新 | `tests/integration/2026-06-07_imx_safetyd_initd.md` |
| stop/status | 已通过：`stop` 后进程停止，PID 文件删除，`status` 显示 stopped | `tests/integration/2026-06-07_imx_safetyd_initd.md` |
| restart | 已通过：`restart` 后再次进入 running | `tests/integration/2026-06-07_imx_safetyd_initd.md` |
| 开机自启 | 已确认条件具备：`/etc/init.d/rcS` 会执行 `/etc/init.d/S??*`；未重启实测 | `tests/integration/2026-06-07_imx_safetyd_initd.md` |

## 11.7 Task07-D 后续增强测试计划（可选，不阻塞 MVP）

Task07-D 属于后续增强测试，不阻塞当前 MVP。

### Task07-D1 原生 libcurl HTTP client 测试

- 编译确认 sysroot 中 libcurl 可用。
- `/health` GET。
- `/api/infer/vision` multipart POST。
- `/api/events` JSON POST。
- timeout / HTTP 非 200 / 网络断开错误码记录。
- 与当前 curl 子进程方案结果一致。

### Task07-D2 原生 V4L2 抓拍测试

- `/dev/video1` 能打开。
- 查询 UVC 能力。
- 设置 MJPG 640x480。
- 成功抓取一帧 JPEG。
- 与 v4l2-ctl 抓拍文件大小/格式对照。
- 摄像头不存在/被占用时能降级。

### Task07-D3 模块化回归测试

- NORMAL / VERIFY。
- OPi5 offline。
- Flask spool / flush。
- loop 模式。
- status JSON。
- Dashboard 展示。
- `control_allowed=false`。

## 11.8 风险矩阵

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| i.MX6ULL SDK 难配 | 中 | 高 | 先 hello，必要时板端原生编译 |
| 摄像头格式不兼容 | 中 | 中 | MJPEG 优先，降分辨率，测试图回退 |
| RKNN 模型转换卡住 | 高 | 中 | 先 mock 和已有 demo |
| 供电不稳 | 中 | 高 | 多路 DC-DC，舵机/负载独立供电 |
| Linux 软实时质疑 | 中 | 中 | 强调人机尺度、watchdog、默认安全态 |
| Dashboard schema 破坏 | 中 | 中 | `raw_json` 兼容，增量迁移 |

## 11.9 答辩前检查清单

- [ ] Dashboard 可打开。
- [ ] 至少一条完整事件可展示。
- [ ] 至少一张抓拍图可展示。
- [ ] AI JSON 中 `control_allowed=false`。
- [ ] C 版 `imx_safetyd --mode once/loop/flush` 可演示。
- [ ] 一组 GPIO/I2C/PWM 波形或截图可展示。
- [ ] 断网/OPi5 离线降级有说法或演示。
- [ ] legacy 迁移叙事不说“失败”，说“工程迭代”。
