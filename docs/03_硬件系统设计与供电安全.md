# 03 硬件系统设计与供电安全

## 3.1 硬件模块总览

| 层级 | 模块 | 作用 | 功能层 |
|---|---|---|---|
| 主控 | STM32F103C8T6 核心板 | 唯一主控，采集、判断、显示、执行器控制 | 基础安全闭环 |
| 显示交互 | OLED、4 位按键、RGB KY-016、蜂鸣器 | 人机交互和状态可视化 | 基础安全闭环 |
| 传感器 | MQ-2、火焰、PIR/门磁、温湿度 | 本地安全输入 | 基础安全闭环 |
| 健康监测 | GY-521 MPU6050 | 震动、搬动、敲击检测 | 边缘视觉取证 |
| 执行器 | 风扇、继电器、水泵 | 通风、断电保护模拟、喷淋模拟 | 基础安全闭环 |
| 视觉联网 | ESP32-CAM | 抓拍、Web、上传、AI 中转 | 边缘视觉取证 + 远程管理 |
| 服务器 | PC/笔记本运行 Flask/FastAPI | 事件接收、图片存储、AI 请求中转 | 远程管理 + AI 解释 |
| 结构 | 亚克力板、热熔胶、扎带、托盘 | 固定、防水、演示模型 | 基础安全闭环 |

---

## 3.2 推荐供电拓扑

当前 P0 bring-up 阶段，已采用面包板红/蓝电源轨分配 `3.3V/GND`：STM32 3.3V 接面包板红色电源轨，STM32 GND 接面包板蓝色电源轨，AD 按键、蜂鸣器、RGB 等小模块从面包板电源轨取电。信号线直接接 STM32 GPIO，或通过面包板中间区域转接，禁止误插到电源轨。

当前阶段可临时由 ST-Link 3.3V 给 STM32 和小功耗模块供电，仅限 PC13、AD 按键、蜂鸣器、RGB 等 P0 小负载验证。后续风扇、水泵、继电器、ESP32-CAM 等大电流或峰值电流模块不得由 ST-Link 或 STM32 GPIO 直接供电。

```text
稳定 5V 3A 电源
 ├─ STM32F103C8T6 核心板 5V 输入
 │    └─ 板载稳压输出 3.3V 给低功耗传感器/逻辑模块
 ├─ ESP32-CAM 5V 输入，单独支路，线尽量短
 ├─ 水泵/风扇/继电器 5V 支路，经 MOS 管/驱动板/继电器模块控制
 └─ 所有 GND 必须共地
```

如部分外设确实需要 12V，只允许在独立测试阶段使用 12V 锂电池或 12V 适配器，并通过继电器/MOS 与 STM32 逻辑隔离；最终课堂演示建议低压化。

---

## 3.3 供电安全表

| 模块 | 推荐电源 | 注意事项 |
|---|---|---|
| STM32F103C8T6 | 5V 输入或 3.3V 稳压 | P0 小负载可临时用 ST-Link 3.3V；不要从 STM32 3.3V 给大电流模块供电 |
| ESP32-CAM | 5V 独立支路 | Wi-Fi 和拍照时峰值电流大，建议 5V 3A 总电源、短线、旁路电容 |
| MQ-2 | 多数模块 5V | AO 输出可能接近 5V，进 STM32 ADC 前必须测量，必要时分压 |
| OLED | 3.3V/5V 模块 | I2C 上拉电平要与 STM32 兼容 |
| GY-521 MPU6050 | 3.3V/5V 模块 | 与 OLED 共 I2C，注意地址和上拉 |
| 继电器 | 5V 模块 | IN 脚需确认 3.3V 能触发，线圈需带续流保护 |
| 风扇/水泵 | 5V 支路 | 不可由 GPIO 直接供电，必须经过驱动板或 MOS 管 |
| 蜂鸣器模块 | 3.3V 小负载 | 当前实测为低电平触发，PB5 初始电平建议 High，避免上电误响 |
| RGB KY-016 | 3.3V GPIO | P0 已验证 PB0/PB1/PB10 可控制 R/G/B；确认共阴/共阳和限流电阻 |

---

## 3.4 共地要求

STM32、ESP32-CAM、传感器、电机驱动板、继电器模块、5V 电源必须共地。否则会出现 UART 通信乱码、ESP32-CAM 心跳失败、传感器电平不稳定、水泵/继电器误动作、STM32 复位等问题。

建议所有 GND 回到同一电源负极，电机支路的电流回路尽量不要穿过 STM32 小信号区域。

---

## 3.5 防止水泵/继电器导致 STM32 复位

| 风险 | 预防措施 |
|---|---|
| 水泵启动电流拉低 5V | 使用 5V 3A 电源，水泵独立支路，线缆加粗，靠近水泵加 470uF-1000uF 电解电容 |
| 电机反电动势 | 使用带续流二极管/TVS 的驱动板，或在电机两端加续流保护 |
| 继电器线圈干扰 | 使用带光耦/续流二极管模块，继电器和 STM32 分支供电 |
| 地弹噪声 | 星形接地或粗地线，电机地线不要和 I2C/UART 细线缠绕 |
| 水泵空转 | 软件限时运行，每次喷淋 1-3 秒，冷却 10 秒以上 |
| 水路漏水 | 托盘、水管扎紧、喷头固定朝托盘，主控板抬高并远离水路 |

---

## 3.6 MQ-2 AO 电平安全

MQ-2 模块常用 5V 供电，其 AO 模拟输出可能超过 STM32 ADC 的 3.3V 上限。

处理流程：上电后用万用表测 AO 最大电压。若可能超过 3.3V，必须加分压。推荐分压：上拉电阻 10k、下拉电阻 20k 左右，使 5V 约变为 3.3V。软件中 ADC 只作为趋势和阈值判断，不作为精密浓度测量。

```text
MQ-2 AO ── R1=10k ──┬── STM32 ADC
                     │
                   R2=20k
                     │
                    GND
```

---

## 3.7 水路结构安全

```text
水箱/储水容器 → 5V 小水泵 → 水管 → 喷头 → 托盘
                                     ↓
                                只喷向托盘

STM32、ESP32-CAM、电源模块必须远离水路并抬高固定。
```

前期可先空泵或 LED 模拟，不马上真实喷水。真实喷水只在托盘、防水、固定均可靠后进行。水泵运行时间限制 1-3 秒。答辩现场准备纸巾、绝缘胶带和备用断电方案。

---

## 3.8 推荐引脚分配表

当前 P0 已验证引脚优先级高于早期推荐表。PA13/PA14 必须保留 SWD；AD 四位按键使用 PA0/ADC1_IN0；蜂鸣器使用 PB5 且低电平触发；RGB 使用 PB0/PB1/PB10。

### P0 已验证与预留引脚

| STM32 引脚 | 当前用途 | 状态 | 说明 |
|---|---|---|---|
| PA13 | SWDIO | 已验证/保留 | ST-Link 下载调试，禁止改作普通 GPIO |
| PA14 | SWCLK | 已验证/保留 | ST-Link 下载调试，禁止改作普通 GPIO |
| PC13 | 板载 LED | 已验证 | 低电平点亮，用于 P0 心跳 |
| PA0 | ADC1_IN0 | 已验证 | AD 四位按键模块 AD 输出，不是 4 路独立 GPIO 按键 |
| PB5 | 蜂鸣器 | 已验证 | 有源蜂鸣器低电平触发，`RESET` 响、`SET` 不响，CubeMX 初始电平建议 High |
| PB0 | RGB_R | 已验证 | KY-016 红色通道 |
| PB1 | RGB_G | 已验证 | KY-016 绿色通道 |
| PB10 | RGB_B | 已验证 | KY-016 蓝色通道 |
| PA9 | USART1_TX | 预留/待验证 | USB-TTL 未到货，P0-01 暂缓 |
| PA10 | USART1_RX | 预留/待验证 | USB-TTL 未到货，P0-01 暂缓 |
| PA2 | USART2_TX | 预留 | 后续 STM32 发给 ESP32-CAM |
| PA3 | USART2_RX | 预留 | 后续 ESP32-CAM 发给 STM32 |
| PB6 | I2C1_SCL | 预留/待验证 | 后续 OLED / MPU6050 |
| PB7 | I2C1_SDA | 预留/待验证 | 后续 OLED / MPU6050 |

蜂鸣器代码建议使用如下逻辑，避免把低电平触发模块误写成高电平触发：

```c
#define BUZZER_ON_LEVEL   GPIO_PIN_RESET
#define BUZZER_OFF_LEVEL  GPIO_PIN_SET
```

| 功能 | 模块 | STM32 引脚 | 外设/模式 | 说明 |
|---|---|---|---|---|
| 调试 TX | USB-TTL | PA9 | USART1_TX | 保留调试日志 |
| 调试 RX | USB-TTL | PA10 | USART1_RX | 可选串口命令 |
| ESP RX | ESP32-CAM RX | PA2 | USART2_TX | STM32 发给 ESP |
| ESP TX | ESP32-CAM TX | PA3 | USART2_RX | ESP 发给 STM32 |
| OLED SCL | OLED | PB6 | I2C1_SCL | 与 MPU6050 共总线 |
| OLED SDA | OLED | PB7 | I2C1_SDA | 注意上拉 |
| MPU6050 SCL | GY-521 | PB6 | I2C1_SCL | 地址通常 0x68 |
| MPU6050 SDA | GY-521 | PB7 | I2C1_SDA | 与 OLED 共线 |
| MQ-2 AO | 烟雾 | 待分配 | ADC | 必须确认电压不超过 3.3V；当前 PA0 已用于 AD 按键 |
| 火焰 DO | 火焰模块 | 待分配 | GPIO_IN | P0-04 待验证，避免占用 PB0/PB1/PB10/PB5 |
| DHT DATA | 温湿度 | 待分配 | GPIO 双向 | P0-04 后续验证，避免占用已验证输出引脚 |
| PIR OUT | PIR | PB12 | GPIO_IN | 可选人体触发 |
| 门磁 | Reed | PB13 | GPIO_IN_PULLUP | 开门检测 |
| 按键 K1-K4 | AD 四位按键 | PA0 | ADC1_IN0 | 已验证，通过 ADC 阈值解码 |
| 蜂鸣器 | Buzzer | PB5 | GPIO | 已验证，低电平触发 |
| RGB R | KY-016 | PB0 | GPIO | 已验证 |
| RGB G | KY-016 | PB1 | GPIO | 已验证 |
| RGB B | KY-016 | PB10 | GPIO | 已验证 |
| 风扇控制 | MOS/继电器 | PB8 | GPIO_OUT | 驱动模块输入 |
| 水泵控制 | 驱动板/MOS | PB9 | GPIO_OUT | 禁止 GPIO 直驱水泵 |
| 继电器 | Relay | 待分配 | GPIO_OUT | PB10 当前已用于 RGB_B；继电器必须经驱动模块 |
| 板载 LED | PC13 | LED | GPIO_OUT | 仅作状态指示 |

---

## 3.9 备用引脚

| 功能 | 备用方案 |
|---|---|
| ESP32-CAM UART | PB10/PB11 USART3，若 USART2 冲突可改用 |
| ADC 输入 | PA1/PA4/PA5，前提是不与按键/其他功能冲突 |
| 按键 | PB3/PB4/PA15 可用，但需关闭 JTAG，优先不用 |
| 执行器 | PB8/PB9/PB10/PB11/PA5 等 GPIO 均可 |

---

## 3.10 引脚冲突和禁用注意

| 引脚/资源 | 注意事项 |
|---|---|
| PA13/PA14 | SWD 调试接口，不占用 |
| NRST | 复位脚，不占用 |
| BOOT0 | 启动模式脚，不接传感器 |
| PA11/PA12 | 可能用于 USB，尽量保留 |
| PA15/PB3/PB4 | 默认与 JTAG 相关，新手优先避开 |
| PC13 | 驱动能力弱，仅用于板载 LED |
| PB6/PB7 | I2C 共线，OLED 和 MPU6050 地址不能冲突 |
| USART1 | 建议保留调试，不接 ESP32-CAM |

---

## 3.11 实体布局建议

```text
┌──────────────────────── 设备柜/巡检盒模型 ────────────────────────┐
│  ESP32-CAM 45°俯视异常区域                                          │
│                                                                     │
│  [火焰传感器]   [MQ-2]       [PIR/门磁]                              │
│       ↓            ↓             ↓                                  │
│  ┌──────────── 异常演示区：烟雾/火焰/开门/震动 ────────────┐           │
│  │                                                         │           │
│  └─────────────────────────────────────────────────────────┘           │
│                                                                     │
│  GY-521 固定在底板/侧壁       DHT 放在通风处                           │
│                                                                     │
│  水箱 + 水泵 + 水管 + 喷头 + 托盘：远离主控和电源                       │
├─────────────────────────────────────────────────────────────────────┤
│ 前面板：OLED | K1 K2 K3 K4 | RGB | 蜂鸣器                              │
│ 后部/底部：STM32 | 驱动板 | 电源 | 线缆固定                              │
└─────────────────────────────────────────────────────────────────────┘

旁边放置：笔记本运行 Flask 服务器 + 手机浏览器查看 Web Dashboard
```

---

## 3.12 网络/服务器硬件说明

服务器和 Web Dashboard 不需要额外嵌入式硬件。答辩现场网络环境推荐如下：

| 组件 | 建议方案 |
|---|---|
| Wi-Fi 热点 | 手机热点或便携路由器，避免依赖教室公共网络 |
| 服务器 | 笔记本电脑运行 Flask/FastAPI，局域网 IP |
| 手机端 | 同一 Wi-Fi 下手机浏览器访问服务器 IP |
| 云端 AI | 笔记本通过手机热点 4G 访问云端 API；或使用预录结果 |

即使网络环境不佳，STM32 本地闭环仍可独立演示。Web/AI 部分可通过预录视频补充。
